name: Run Tests

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Test prpl
        run:  sudo bash install.sh &&cd test && bash test.sh
